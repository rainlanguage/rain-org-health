# Generated by roh-scan (serde_json::to_string_pretty); not hand-formatted.
site/health.json
# Standalone JS/TS is formatted by deno (the denofmt hook), not prettier — the
# two disagree on JS style, so scope prettier to markup and let deno own tests.
test/
# Vendored third-party libraries, shipped exactly as published upstream.
# Formatting them would rewrite ~1.9MB of minified code we do not own, make the
# diff unreviewable, and desync the file from the version it claims to be.
site/vendor/
